


<% @products.each do |product| %>
<div class="jumbotron">
      <div class="container">
        <h1><%=product.name%></h1>
        <p><%=product.description %> </p>
        <p><%=link_to "See product",  product_path(product.id) %></a></p>
      </div>
    </div>
  <% end %>
